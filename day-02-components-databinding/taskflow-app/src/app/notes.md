# Day 2 — Components & Data Binding

## Topics Covered

- Angular component anatomy (@Component decorator)
- Standalone components
- Interpolation {{ }}, property binding [ ], event binding ( )

## What I Built

- `UserCardComponent` — displays user name, role, online status toggle

## Key Takeaways

- @Component ≈ a C# attribute, configures the class as an Angular component
- [property] binds a DOM property, (event) binds a DOM event, {{ }} displays text
